{
  "name": "radar-vital-mobile",
  "version": "16.6.3",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:www": "node ./scripts/prepare-www.mjs",
    "sync:android": "npm run prepare:www && npx cap sync android",
    "build:android": "npm run prepare:www && npx cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/app": "^8.1.1",
    "@capacitor/cli": "^8.3.4",
    "@capacitor/core": "^8.3.4",
    "@capacitor-community/http": "^1.4.1",
    "@capacitor-community/bluetooth-le": "^8.1.3"
  },
  "devDependencies": {}
}
