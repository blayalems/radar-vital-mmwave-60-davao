/**
 * Shared storage keys constants to prevent key drift across components,
 * services, and functional HTTP interceptors.
 */
export const API_BASE_KEY = 'rvt-api-base';
export const TOKEN_KEY = 'rvt-pair-token';
